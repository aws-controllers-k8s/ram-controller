---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: resourceshareinvitations.ram.services.k8s.aws
spec:
  group: ram.services.k8s.aws
  names:
    kind: ResourceShareInvitation
    listKind: ResourceShareInvitationList
    plural: resourceshareinvitations
    singular: resourceshareinvitation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.resourceShareInvitationARN
      name: INVITATION-ARN
      type: string
    - jsonPath: .status.resourceShareARN
      name: SHARE-ARN
      type: string
    - jsonPath: .status.status
      name: STATUS
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ResourceShareInvitation is the Schema for the ResourceShareInvitations
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ResourceShareInvitationSpec defines the desired state of ResourceShareInvitation.

              Describes an invitation for an Amazon Web Services account to join a resource
              share.
            properties:
              clientToken:
                description: |-
                  Specifies a unique, case-sensitive identifier that you provide to ensure
                  the idempotency of the request. This lets you safely retry the request without
                  accidentally performing the same operation a second time. Passing the same
                  value to a later call to an operation requires that you also pass the same
                  value for all other parameters. We recommend that you use a UUID type of
                  value. (https://wikipedia.org/wiki/Universally_unique_identifier).

                  If you don't provide this value, then Amazon Web Services generates a random
                  one for you.

                  If you retry the operation with the same ClientToken, but with different
                  parameters, the retry fails with an IdempotentParameterMismatch error.
                type: string
              resourceShareInvitationARN:
                description: |-
                  The Amazon Resource Name (ARN) (https://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html)
                  of the invitation that you want to accept.

                  WARNING: Deleting this resource is destructive. Because there is no
                  API to "un-accept" a resource share invitation, deleting the
                  ResourceShareInvitation custom resource calls DisassociateResourceShare
                  on the underlying resource share. This removes the receiver account
                  from the share and revokes its access to every resource that the share
                  grants. The invitation is not merely forgotten by the controller; the
                  account is actively detached from the shared resources. Only delete this
                  resource if you intend to give up access to the shared resources.
                type: string
                x-kubernetes-validations:
                - message: Value is immutable once set
                  rule: self == oldSelf
            required:
            - resourceShareInvitationARN
            type: object
          status:
            description: ResourceShareInvitationStatus defines the observed state
              of ResourceShareInvitation
            properties:
              ackResourceMetadata:
                description: |-
                  All CRs managed by ACK have a common `Status.ACKResourceMetadata` member
                  that is used to contain resource sync state, account ownership,
                  constructed ARN for the resource
                properties:
                  arn:
                    description: |-
                      ARN is the Amazon Resource Name for the resource. This is a
                      globally-unique identifier and is set only by the ACK service controller
                      once the controller has orchestrated the creation of the resource OR
                      when it has verified that an "adopted" resource (a resource where the
                      ARN annotation was set by the Kubernetes user on the CR) exists and
                      matches the supplied CR's Spec field values.
                      https://github.com/aws/aws-controllers-k8s/issues/270
                    type: string
                  ownerAccountID:
                    description: |-
                      OwnerAccountID is the AWS Account ID of the account that owns the
                      backend AWS service API resource.
                    type: string
                  partition:
                    description: Partition is the AWS partition in which the resource
                      exists or will exist
                    type: string
                  region:
                    description: Region is the AWS region in which the resource exists
                      or will exist.
                    type: string
                required:
                - ownerAccountID
                - region
                type: object
              conditions:
                description: |-
                  All CRs managed by ACK have a common `Status.Conditions` member that
                  contains a collection of `ackv1alpha1.Condition` objects that describe
                  the various terminal states of the CR and its backend AWS service API
                  resource
                items:
                  description: |-
                    Condition is the common struct used by all CRDs managed by ACK service
                    controllers to indicate terminal states  of the CR and its backend AWS
                    service API resource
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about
                        the transition.
                      type: string
                    reason:
                      description: The reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the Condition
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              invitationTimestamp:
                description: The date and time when the invitation was sent.
                format: date-time
                type: string
              receiverARN:
                description: |-
                  The Amazon Resource Name (ARN) (https://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html)
                  of the IAM user or role that received the invitation.
                type: string
              receiverAccountID:
                description: The ID of the Amazon Web Services account that received
                  the invitation.
                type: string
              resourceShareARN:
                description: |-
                  The Amazon Resource Name (ARN) (https://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html)
                  of the resource share
                type: string
              resourceShareAssociations:
                description: |-
                  To view the resources associated with a pending resource share invitation,
                  use ListPendingInvitationResources.
                items:
                  description: |-
                    Describes an association between a resource share and either a principal
                    or a resource.
                  properties:
                    associatedEntity:
                      type: string
                    associationType:
                      type: string
                    creationTime:
                      format: date-time
                      type: string
                    external:
                      type: boolean
                    lastUpdatedTime:
                      format: date-time
                      type: string
                    resourceShareARN:
                      type: string
                    resourceShareName:
                      type: string
                    status:
                      type: string
                    statusMessage:
                      type: string
                  type: object
                type: array
              resourceShareName:
                description: The name of the resource share.
                type: string
              senderAccountID:
                description: The ID of the Amazon Web Services account that sent the
                  invitation.
                type: string
              status:
                description: The current status of the invitation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
